# Raw + derived corpora and the built vector index — reproduce via the ingest + build
# scripts (see README "Setup" and data/README.md). Big; not committed.
data/cricsheet/*/
data/cricsheet/*.zip
data/sports_se/*.json
data/wikipedia/*.json
data/rules/*.json
data/qdrant/

# Runtime artifacts (persisted chat, per-run traces)
data/traces/
data/session/

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# Env / secrets — never commit real keys
.env
*.local

# OS / editor / local tooling
.DS_Store
.claude/

# Personal working docs — not part of this project
money_se_handoff.md
docs/superpowers/
docs/handoff-enhance-legs.md

# Demo video assets — big binaries, not part of the repo
*.mov
*.mp4

# Deploy artifacts (dump + built index) are committed so CI can bake them into the amd64 image.
# ~76MB total; move to Git LFS or release assets later if you want a leaner clone.
